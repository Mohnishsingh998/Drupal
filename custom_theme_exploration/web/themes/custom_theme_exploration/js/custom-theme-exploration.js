/**
 * @file
 * Custom Theme Exploration behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.customThemeExploration = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
